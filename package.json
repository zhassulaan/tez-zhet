{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build -p 3000",
    "dev": "nuxt dev -p 3000",
    "generate": "nuxt generate -p 3000",
    "preview": "nuxt preview -p 3000",
    "postinstall": "nuxt prepare -p 3000"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/node": "^18",
    "nuxt": "^3.5.2"
  },
  "dependencies": {
    "nuxt-swiper": "^1.1.0",
    "sass": "^1.63.2",
    "swiper": "^9.3.2",
    "vue-the-mask": "^0.11.1"
  }
}
